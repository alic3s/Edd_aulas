lista = [20, 40, 50, 30, 10]

maior = max(lista)
menor = min(lista)

for indice, item in enumerate(lista):
   if item == maior:
       print(f"O maior valor encontrado foi {maior} na posição {indice}")
   if item == menor:
       print(f'O menor valor encontrado foi {menor} na posição {indice}')
